# AI-Docs-Agent
Parses `.docx`, `.txt`, `.ppt`/`.pptx`, and `.xlsx` files, stores a whitespace-stripped, base64-encoded copy of the extracted content in Postgres, and returns an AI-generated summary using Groq's API
